# AuroraOS 系统架构设计文档

## 1. 概述

AuroraOS 是一个基于 x86_64 架构的自主研发操作系统内核，采用混合内核（Hybrid Kernel）设计，支持多任务调度、虚拟内存管理、虚拟文件系统、POSIX 信号机制、ELF 可执行文件加载、SMP 多核、动态模块加载和硬件性能监控。

- **版本**: 3.4.0
- **目标架构**: x86_64 (AMD64)
- **内核类型**: 混合内核（宏内核 + 可加载模块）
- **启动方式**: Multiboot1 (GRUB2) / UEFI
- **许可证**: MIT

> **参考项目**: 本项目受 [CoolPotOS](https://cpos.plos-clan.org) 启发，在 procfs 设计、IRQ 追踪、内核模块签名、CMake 构建系统等方面借鉴了 CoolPotOS 的架构理念。
>
> **配套文档**: [架构可视化文档](architecture-visual.md) | [多媒体演示指南](demo-guide.md) | [模块功能说明](modules.md) | [API 文档](api.md)

## 2. 系统架构图

```
+------------------------------------------------------------------+
|                        用户空间 (User Space)                       |
|  +------------------+  +------------------+  +------------------+ |
|  |   Shell 进程     |  |   ELF 应用程序   |  |   用户态程序     | |
|  +------------------+  +------------------+  +------------------+ |
+------------------------------------------------------------------+
                            | 系统调用 (syscall)
                            | [seccomp 过滤器]
+------------------------------------------------------------------+
|                       内核空间 (Kernel Space)                      |
|                                                                   |
|  +---------------------+    系统调用层    +---------------------+  |
|  |    syscall.c        | <--------------> |  syscall_entry.c    |  |
|  |  (系统调用分发器)    |     [seccomp]   |  (MSR 入口设置)     |  |
|  +---------------------+                  +---------------------+  |
|           |                                                        |
|  +--------+--------+--------+--------+--------+--------+--------+ |
|  |        |        |        |        |        |        |        | |
|  | VFS    | 进程   | 内存   | 信号   | 文件   | 管道   | ELF    | |
|  | 层     | 管理   | 管理   | 机制   | 系统   | 机制   | 加载器 | |
|  +--------+--------+--------+--------+--------+--------+--------+ |
|  | vfs.c  | sched.c| mem.c  |signal.c| fs.c   | pipe.c |elfldr.c| |
|  +--------+--------+--------+--------+--------+--------+--------+ |
|           |                                                        |
|  +--------+--------+--------+--------+--------+--------+--------+ |
|  | 控制台 | 键盘   | 中断   | 异常   | 时钟   | 页表   | 能力   | |
|  | 驱动   | 驱动   | 管理   | 处理   | 中断   | 管理   | 系统   | |
|  +--------+--------+--------+--------+--------+--------+--------+ |
|  |console |keyboard| irq.c  |except.c| pit.c  |pgtbl.c |cap.c   | |
|  +--------+--------+--------+--------+--------+--------+--------+ |
|                                                                   |
|  +--------+--------+--------+--------+--------+--------+--------+ |
|  | SMP    | APIC   | Ext2   | PCI    | VirtIO | VirtIO | ASLR   | |
|  | 多核   | 中断   | 文件   | 总线   | 块设备 | 网络   | 地址   | |
|  | 支持   | 控制   | 系统   | 枚举   | 驱动   | 驱动   | 随机化 | |
|  +--------+--------+--------+--------+--------+--------+--------+ |
|  | smp.c  | apic.c | ext2.c | pci.c  |virtio  |virtio  | aslr.c | |
|  |        |        |        |        |_blk.c  |_net.c  |        | |
|  +--------+--------+--------+--------+--------+--------+--------+ |
|                                                                   |
|  +--------+--------+--------+--------+--------+--------+--------+ |
|  |seccomp | Perf   | Sysctl | Module | 栈保护 | 块设备 | 网络   | |
|  | 系统   | 性能   | 系统   | 动态   | 金丝雀 | 抽象   | 设备   | |
|  | 调用   | 监控   | 控制   | 模块   | 保护   | 层     | 抽象   | |
|  | 过滤   |        | 接口   | 加载   |        |        | 层     | |
|  +--------+--------+--------+--------+--------+--------+--------+ |
|  |seccomp | perf.c |sysctl.c|module.c|stack_  |block_  |netdev  | |
|  |.c      |        |        |        |protect |dev.c   |.c      | |
|  +--------+--------+--------+--------+--------+--------+--------+ |
|                                                                   |
+------------------------------------------------------------------+
                            |
+------------------------------------------------------------------+
|                        硬件抽象层 (HAL)                            |
|  +------------------+  +------------------+  +------------------+ |
|  |   GDT / IDT      |  |   上下文切换      |  |   端口 I/O       | |
|  |   (gdt.S/idt.S)  |  |   (context.S)    |  |   (portio.h)     | |
|  +------------------+  +------------------+  +------------------+ |
+------------------------------------------------------------------+
                            |
+------------------------------------------------------------------+
|                        硬件 (Hardware)                             |
|  x86_64 CPU | VGA 文本模式 | PS/2 键盘 | PIT 定时器 | 物理内存  |
|  SMP 多核   | UEFI GOP     | PCI 总线  | APIC 中断  | 块设备    |
+------------------------------------------------------------------+
```

## 3. 模块划分

### 3.1 核心基础设施

| 模块 | 文件 | 职责 |
|------|------|------|
| 入口 | `entry.S` | 内核入口点，Multiboot 头 |
| 主初始化 | `main.c` | 内核启动流程，各子系统初始化 |
| 日志 | `log.c` | 分级日志输出（DEBUG/INFO/WARN/ERR） |
| 断言 | `include/assert.h` | 运行时断言宏 |
| 自检 | `selftest.c` | 内核自检框架（16 项） |

### 3.2 内存管理

| 模块 | 文件 | 职责 |
|------|------|------|
| 物理内存 | `mem.c` | Buddy 伙伴系统、Slab 分配器、kmalloc/kfree |
| 页表管理 | `pagetable.c` | 4 级页表、COW、页面映射、缺页处理 |
| 内存类型 | `mem.h` | 内存管理接口与数据结构定义 |
| 页表接口 | `pagetable.h` | 页表操作公共接口 |

### 3.3 进程管理

| 模块 | 文件 | 职责 |
|------|------|------|
| 调度器 | `sched.c` | 轮转调度、PID 管理、任务切换、fork/exec、per-CPU 就绪队列 |
| 调度器头 | `sched.h` | 任务结构体、调度器接口 |
| 上下文切换 | `arch/x86_64/context.S` | 汇编级上下文保存与恢复 |
| 用户态入口 | `arch/x86_64/enter_user.S` | 从内核态切换到用户态 |

### 3.4 中断与异常

| 模块 | 文件 | 职责 |
|------|------|------|
| 中断管理 | `irq.c` | IRQ 注册、PIC/IOAPIC 中断控制器初始化、IPI 处理 |
| 中断入口 | `irq_handler.S` | 通用中断处理入口 |
| 异常处理 | `exception.c` | CPU 异常分发处理 |
| 异常入口 | `arch/x86_64/exception_handlers.S` | 异常向量入口 |
| 缺页处理 | `arch/x86_64/pf_handler.S` | #PF 缺页异常入口 |
| 键盘中断 | `arch/x86_64/keyboard_handler.S` | 键盘中断入口 |
| IDT | `arch/x86_64/idt.S` | 中断描述符表设置 |
| GDT | `arch/x86_64/gdt.S` | 全局描述符表设置 |
| TSS | `arch/x86_64/tss.S` | 任务状态段设置 |

### 3.5 设备驱动

| 模块 | 文件 | 职责 |
|------|------|------|
| 控制台 | `console.c` | VGA 文本模式 / 帧缓冲输出、ANSI 转义序列、行编辑 |
| 键盘 | `keyboard.c` | PS/2 键盘驱动、修饰键、E0 前缀处理 |
| 时钟 | `pit.c` | PIT 可编程间隔定时器 |
| 时钟处理 | `pit_handler.c` | PIT 中断处理、调度触发 |

### 3.6 文件系统

| 模块 | 文件 | 职责 |
|------|------|------|
| VFS | `vfs.c` | 虚拟文件系统抽象层 |
| 文件系统 | `fs.c` | 文件系统初始化与挂载 |
| RamFS | `ramfs.c` | 内存文件系统实现 |
| Ext2 | `ext2.c` | ext2 文件系统实现（超级块、inode、目录遍历、直接块+间接块） |
| 文件操作 | `file.c` | 文件描述符管理 |
| 管道 | `pipe.c` | 管道通信机制 |
| 头文件 | `vfs.h`, `fs.h`, `ext2.h` | 文件系统接口定义 |

### 3.7 系统调用

| 模块 | 文件 | 职责 |
|------|------|------|
| 系统调用分发 | `syscall.c` | 系统调用实现与分发（22 个系统调用） |
| 系统调用入口 | `syscall_entry.c` | MSR 寄存器配置 |
| 系统调用号 | `syscall.h` | 系统调用号枚举 |
| 汇编入口 | `arch/x86_64/syscall.S` | syscall 指令入口 |
| 用户态启动 | `arch/x86_64/start_user.S` | 用户态进程启动 |

### 3.8 进程间通信

| 模块 | 文件 | 职责 |
|------|------|------|
| 信号 | `signal.c` | POSIX 信号递送与处理 |
| 信号头 | `signal.h` | 信号号定义、信号处理接口 |

### 3.9 用户界面

| 模块 | 文件 | 职责 |
|------|------|------|
| Shell | `shell.c` | 内置内核 Shell，命令历史、Tab 补全、主题/锁屏/模块管理 |
| 主题 | `include/theme.h` | 配色方案与视觉设计令牌 |
| 布局 | `layout.h` | 控制台布局工具函数 |
| 错误说明 | `explain.c` | 错误码人类可读说明 |

### 3.10 安全

| 模块 | 文件 | 职责 |
|------|------|------|
| 能力系统 | `capability.c` | 基于能力的访问控制 |
| 栈保护 | `stack_protect.c` | 栈金丝雀保护（`-fstack-protector-strong`） |
| ASLR | `aslr.c` | 地址空间布局随机化（xorshift64 PRNG，栈/mmap 随机化） |
| Seccomp | `seccomp.c` | 系统调用访问控制（256 位位图过滤器） |
| SMAP/SMEP | `pagetable.c` | 内核访问保护（CR4 位 20/21），防止内核访问用户空间内存（CoolPotOS 启发） |
| 模块签名 | `module_sign.c` | 内核模块完整性验证（HMAC 风格签名） |

### 3.11 ELF 加载

| 模块 | 文件 | 职责 |
|------|------|------|
| ELF 加载器 | `elfloader.c` | ELF 可执行文件解析与加载 |
| ELF 头 | `elf.h` | ELF 格式结构体定义（含可重定位文件支持） |

### 3.12 SMP 多核支持（v3.0 新增）

| 模块 | 文件 | 职责 |
|------|------|------|
| SMP 初始化 | `smp.c` | ACPI MADT 解析、AP 启动跳板、per-CPU 数据结构 |
| APIC 驱动 | `apic.c` | LAPIC/IOAPIC 初始化、IPI 发送、定时器校准 |
| SMP 头 | `smp.h` | CPU 数据结构、原子自旋锁（lock cmpxchg）、票据锁 |
| APIC 头 | `apic.h` | APIC 寄存器地址与操作接口 |

### 3.13 块设备子系统（v3.0 新增）

| 模块 | 文件 | 职责 |
|------|------|------|
| 块设备抽象 | `block_dev.c` | 块设备注册/查找/读写抽象层 |
| RAM 磁盘 | `ramdisk.c` | 16MB 内存虚拟磁盘，用于 ext2 测试 |
| 块设备头 | `block_dev.h` | 块设备接口定义 |

### 3.14 PCI 与 VirtIO 驱动（v3.0 新增）

| 模块 | 文件 | 职责 |
|------|------|------|
| PCI 总线 | `pci.c` | PCI 总线枚举、配置空间访问、设备发现 |
| VirtIO 块设备 | `virtio_blk.c` | VirtIO 块设备驱动（PCI 传输层 + virtqueue 管理） |
| VirtIO 网络 | `virtio_net.c` | VirtIO 网络设备驱动 |
| 网络设备抽象 | `netdev.c` | 网络设备注册/发送/接收抽象层 |
| 头文件 | `pci.h`, `virtio.h`, `netdev.h` | 设备接口定义 |

### 3.15 性能监控与系统控制（v3.0 新增）

| 模块 | 文件 | 职责 |
|------|------|------|
| 性能计数器 | `perf.c` | 8 类性能计数器（上下文切换、系统调用、缺页、COW 等） |
| 系统控制 | `sysctl.c` | 12 项内置统计项（/proc-like 接口） |
| 性能头 | `perf.h` | 性能计数器接口与事件类型定义 |
| 系统控制头 | `sysctl.h` | sysctl 条目注册与查询接口 |

### 3.16 动态模块加载（v3.0 新增）

| 模块 | 文件 | 职责 |
|------|------|------|
| 模块加载器 | `module.c` | ELF 可重定位文件加载、符号解析、x86_64 重定位（5 种类型） |
| 模块头 | `module.h` | 模块描述符、符号表、加载/卸载接口 |
| 示例模块 | `userspace/mod_sample.c` | 示例内核模块 |

## 4. 技术选型

| 技术领域 | 选型 | 理由 |
|----------|------|------|
| 启动协议 | Multiboot1 / UEFI | GRUB2 广泛支持 + UEFI GOP 帧缓冲 |
| 内存模型 | 4 级分页 (PML4) | x86_64 标准，支持 48 位虚拟地址 |
| 物理内存分配 | Buddy 伙伴系统 | 高效的连续页分配与合并 |
| 内核堆分配 | Slab 分配器 | 小对象高效分配，减少碎片 |
| 进程调度 | VRFair (CFS/EEVDF 启发) + SMP 工作窃取 | 基于 vruntime 的公平调度，per-CPU 就绪队列，负载均衡 |
| 文件系统 | VFS + RamFS + Ext2 | 抽象层支持多种文件系统，ext2 支持持久化 |
| 块设备 | block_dev 抽象层 + ramdisk + VirtIO | 统一块设备接口，支持多种后端 |
| 系统调用 | SYSCALL 指令 (MSR) + seccomp 过滤 | x86_64 快速系统调用标准，256 位位图过滤 |
| 多核支持 | SMP + APIC + 原子自旋锁 | MADT 解析，lock cmpxchg 原子操作 |
| 安全 | ASLR + 栈金丝雀 + NX + COW + seccomp | 多层防御纵深 |
| 性能监控 | TSC 频率校准 + 8 类计数器 | 低开销内核性能分析 |
| 模块化 | ELF 可重定位文件动态加载 | 内核功能热插拔，5 种 x86_64 重定位类型 |
| 编译工具链 | x86_64-elf-gcc | 交叉编译，无宿主依赖 |
| 链接脚本 | 自定义 linker.ld | 精确控制内核内存布局 |
| 构建系统 | GNU Make | 简单通用，支持 uefi/modules 目标 |

## 5. 内存布局

```
物理内存布局:
  0x00000000 - 0x00000FFF : IVT / BDA (保留)
  0x00001000 - 0x0009FFFF : 常规内存 (可用)
  0x000A0000 - 0x000FFFFF : VGA / BIOS (保留)
  0x00100000 - 0x3FFFFFFF : 内核 + 可用内存 (~1GB)

虚拟地址空间:
  0x00000000 - 0x00007FFFFFFFFFFF : 用户空间 (128TB)
  0xFFFF800000000000 - 0xFFFFFFFFFFFFFFFF : 内核空间 (128TB)
  0xFFFFFFFF80000000 : 内核代码段 (高半核)

ASLR 随机化:
  mmap 基址: 0x70000000000 + random(0~1GB)
  栈基址:   0x7FFFF0000000 + random(0~1GB)
```

## 6. 启动流程

```
1. GRUB2 / UEFI 加载 kernel.elf
2. entry.S: 设置栈指针，跳转到 kmain
3. kmain():
   a. serial_init()         - 初始化串口 (COM1 115200)
   b. stack_protect_init()   - 初始化栈金丝雀
   c. console_init()         - 初始化 VGA 控制台 / 帧缓冲控制台
   d. boot_print_logo()      - 显示 Aurora 极光 Logo
   e. phys_mem_init()        - 初始化物理内存管理 (E820/MB1/MB2/UEFI)
   f. slab_init()            - 初始化 Slab 分配器
   g. aslr_init()            - 初始化 ASLR 随机数生成器
   h. page_table_init()      - 初始化 4 级页表 (NX 启用)
   i. scheduler_init()       - 初始化调度器 (per-CPU 就绪队列)
   j. syscall_init()         - 配置 MSR 系统调用
   k. irq_init()             - 初始化 IDT + PIC/IOAPIC + 键盘
   l. smp_init()             - SMP 初始化，解析 ACPI MADT，启动 AP
   m. rodata_protect()       - 标记只读数据段为只读
   n. pit_init(100)          - 初始化 PIT 定时器 (100 Hz)
   o. perf_init()            - 初始化性能计数器 (TSC 频率校准)
   p. sysctl_init()          - 初始化系统控制接口 (12 项统计)
   q. ramdisk_init() + fs_init() - 创建 RAM 磁盘，挂载 ext2/ramfs
   r. module_init()          - 初始化模块加载器 (内核符号表)
   s. kernel_selftest()      - 运行 16 项内核自检
   t. shell_main()           - 启动 Shell
```

## 7. 关键设计决策

### 7.1 Copy-On-Write (COW)
- fork() 时父子进程共享物理页，标记为只读
- 写入时触发缺页异常，复制页面
- ref_count 跟踪共享计数，为 0 时释放物理页

### 7.2 内核抢占
- 当前采用协作式调度（yield）
- PIT 定时器中断触发调度检查
- SMP 支持工作窃取（work stealing）负载均衡
- 未来可扩展为抢占式

### 7.3 安全架构
- 用户/内核地址空间隔离（高半核）
- 页表保护：用户模式不可访问内核页
- NX 位防止用户态代码执行
- ASLR 地址空间布局随机化（栈/mmap 随机化，xorshift64 PRNG）
- 栈金丝雀保护（`-fstack-protector-strong`，随机 canary 值）
- Seccomp 系统调用过滤（256 位位图，拒绝返回 -EPERM）
- 能力系统提供细粒度访问控制
- ELF 入口点验证防止任意地址执行
- ELF 段边界验证防止越界内存访问
- VFS 路径遍历防护（拒绝 "." 和 ".." 组件）
- phys_to_virt 身份映射范围验证（0-1GB）

### 7.4 SMP 多核架构
- ACPI MADT 表解析，自动检测 CPU 核心数
- AP 启动跳板代码（trampoline at 0x8000）
- per-CPU 数据结构（cpu_data），GS 段基址快速访问
- per-CPU 就绪队列（per_cpu_rq[MAX_CPUS]），支持工作窃取
- 原子自旋锁（lock cmpxchg + pause）替代 CLI/STI
- 票据锁（ticket lock）提供公平、可扩展的替代方案
- IPI 中断处理（reschedule 0xFE、TLB shootdown 0xFD）
- 禁用 PIC，启用 IOAPIC 进行中断路由

### 7.5 模块系统
- 支持 ELF 可重定位文件（ET_REL）加载
- 5 种 x86_64 重定位类型：R_X86_64_64, R_X86_64_PC32, R_X86_64_32, R_X86_64_32S, R_X86_64_PLT32
- 预注册 20+ 内核符号（kmalloc, vfs_open, memcpy 等）
- 模块生命周期：UNLOADED → LOADING → LIVE → UNLOADING → UNLOADED
- 模块间依赖管理（deps 链表）
- 模块本地符号表（可被其他模块引用）

### 7.6 性能监控
- 8 类性能计数器：上下文切换、系统调用计数/延迟、缺页、COW、内存分配、释放、中断计数
- **IRQ 向量追踪**: 256 向量中断计数器，支持 `/proc/interrupts` 导出
- RDTSC 时间戳计数器，PIT 辅助频率校准
- 延迟统计：最小/最大/平均延迟
- 集成到调度器、系统调用、缺页处理、内存分配、中断处理路径
- `perf` 命令显示统计，`perf reset` 重置计数器

### 7.7 procfs 虚拟文件系统（受 CoolPotOS 启发）
- `/proc/cpuinfo` — CPU 信息
- `/proc/meminfo` — 内存统计
- `/proc/uptime` — 系统运行时间
- `/proc/version` — 内核版本
- `/proc/mounts` — 挂载点信息
- `/proc/interrupts` — IRQ 向量计数（受 CoolPotOS /proc/interrupts 启发）
- `/proc/filesystems` — 支持的文件系统类型
- `/proc/cmdline` — 内核命令行参数
- `/proc/kmsg` — 内核日志环形缓冲区（受 CoolPotOS 内核日志子系统启发）
- `/proc/self/stat` — 当前进程状态

## 8. 与 CoolPotOS 架构对比

| 特性 | AuroraOS | CoolPotOS | 说明 |
|------|----------|-----------|------|
| 内核类型 | 混合内核 | 混合内核 | 均支持内核模块动态加载 |
| 目标架构 | x86_64 | x86_64, riscv64, loongarch64, aarch64 | CoolPotOS 多架构支持更广 |
| 调度器 | VRFair (CFS/EEVDF 启发) | EEVDF | 均采用公平调度策略 |
| 构建系统 | Makefile + CMake | CMake | 均支持 CMake 构建 |
| 代码质量 | clang-format + clang-tidy | clang-format + clang-tidy | 均采用静态分析工具 |
| 模块签名 | HMAC 风格 | ECC 密钥验证 | 不同签名方案 |
| procfs | 10 项 | 丰富 | 均受 Linux procfs 启发 |
| 文件系统 | VFS + RamFS + EXT2 | VFS + procfs + pipefs + devtmpfs + squashfs | CoolPotOS 文件系统类型更丰富 |
| 终端 | VGA + 帧缓冲 | flanterm 终端渲染 | 不同终端实现方案 |
| 容器化 | Dockerfile | Dockerfile | 均支持可复现构建 |
| 70+ POSIX syscall | 否 | 是 | CoolPotOS 可运行 gcc/lua/bash |
| 多平台引导 | BIOS + UEFI | UEFI (Limine) | 启动方式不同 |

## 9. 未来规划

### 短期（v3.1）
- **devtmpfs**: 设备文件系统，自动管理 `/dev` 目录
- **squashfs**: 只读压缩文件系统，用于 rootfs 镜像
- **/proc/<pid>/maps**: 进程内存映射信息
- **更多 POSIX 系统调用**: 扩展至 30+ 系统调用

### 中期（v3.5）
- **多架构支持**: 初步支持 riscv64 架构
- **NVMe 驱动**: 内核模块形式的 NVMe SSD 驱动
- **sysfs**: 内核对象信息导出（设备、总线、驱动）
- **内核模块独立编译**: 支持 `.km` 格式模块，类似 CoolPotOS 模块系统

### 长期（v4.0）
- **完整 POSIX 兼容层**: 运行 Linux 静态链接二进制文件
- **多架构完备支持**: aarch64, loongarch64
- **DRM/KMS 框架**: 基本图形支持
- **网络协议栈**: TCP/IP 基础实现